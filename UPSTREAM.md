# Upstream

Volvelle CLI originated as a fork of
[`caelestia-dots/cli`](https://github.com/caelestia-dots/cli).

The local `upstream` remote tracks that repository so useful changes can be
selected without treating upstream branding or configuration paths as part of
Volvelle's public interface.
